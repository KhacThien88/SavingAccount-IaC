- hosts: "{{ passed_in_hosts }}"
  become: true
  tasks:
    - name: Copy SSH private key to remote machine
      copy:
        src: /var/lib/jenkins/.aws
        dest: /root
        owner: root
        group: root
        mode: 0600
   